import {
  ScreenStatusEnum,
  ScreenTypeEnum,
  TheaterStatusEnum,
} from "@adarsh-tickets/shared";

export const THEATER_STATUS = Object.values(TheaterStatusEnum);

export const SCREEN_STATUS = Object.values(ScreenStatusEnum);

export const SCREEN_TYPES = Object.values(ScreenTypeEnum);
